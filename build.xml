<?xml version="1.0" encoding="UTF-8"?>
<project default="xar" name="wmata">
    <property name="build.dir" value="build"/>
    <target name="xar">
        <mkdir dir="${build.dir}"/>
        <zip basedir="." destfile="${build.dir}/wmata-0.2.xar" excludes="${build.dir}/ build.xml"/>
    </target>
</project>